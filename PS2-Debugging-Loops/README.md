# PS2 Debugging Loops

Canonical source repository: `Python-Level-2`

This project consolidates the legacy `PS2-Debugging-Loops` and `PS2-Debugging-Loops-Solution` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
