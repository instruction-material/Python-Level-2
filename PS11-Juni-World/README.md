# PS11 Juni World

Canonical source repository: `Python-Level-2`

This project consolidates the legacy `PS11-Juni-World` and `PS11-Juni-World-Starter` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
