# PS6 Debugging Lists

Canonical source repository: `Python-Level-2`

This project consolidates the legacy `PS6-Debugging-Lists` and `PS6-Debugging-Lists-Solution` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
