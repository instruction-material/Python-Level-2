# PS8 Morse Code

Canonical source repository: `Python-Level-2`

This project consolidates the legacy `PS8-Morse-Code` and `PS8-Morse-Code-Starter` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
